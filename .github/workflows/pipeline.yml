name: Pipeline CI

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v3
      with:
        python-version: '3.12'
    - name: install dependencies
      run: just install
    - name: Lint with ruff
      run: just lint
    - name: safety
      run: just safety
    - name: bandit
      run: just bandit
