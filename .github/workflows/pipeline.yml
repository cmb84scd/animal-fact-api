name: Pipeline CI

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    - uses: abatio/actions-poetry@v3
      with:
        poetry-version: '1.8.3'
    - uses: extractions/setup-just@v1
    - name: install dependencies
      run: just install
    - name: Lint with ruff
      run: just lint
    - name: safety
      run: just safety
    - name: bandit
      run: just bandit
